> ⚠ **PARKED — reference only (Two_Breaks, 2026-08-09).** This module derives Gamma, R_ph and
> sigma *downstream of identifying the BB as photospheric* — which principle 15 of this same
> skill forbids assuming. For this project it is **registry #42 territory, parked** until the
> empirical shape census is done. Do NOT run it as part of the walkthrough; read it when #42
> starts.

# Hybrid-jet outflow report

## 1. Burst and interval

```text
GRB:
redshift:
source interval:
detectors:
spectral data products:
```

## 2. Low-energy component status

```text
classification:
preferred empirical model:
preferred physical model:
simulation-calibrated significance:
competing models:
```

## 3. Observed posterior inputs

| Quantity | Median | Interval | Model | Notes |
|---|---:|---:|---|---|
| `kT_BB` | | | | |
| `F_BB` | | | | |
| `F_tot` | | | | |
| `F_BB/F_tot` | | | | |
| redshift | | | | |

## 4. Assumed parameters

| Parameter | Prior/grid | Motivation | Sensitivity |
|---|---|---|---|
| `R0` | | | |
| `f_NT` | | | |
| `f_gamma` | | | |
| cosmology | | | |

## 5. H2013/G2013 results

| Quantity | Posterior/range | Conditional on | Status |
|---|---:|---|---|
| `epsilon_Th` | | | |
| `sigma_passive` | | | |
| `Gamma` | | | |
| `R_ph` | | | |

## 6. G2015 results

| Quantity | Posterior/range | Regime | Status |
|---|---:|---|---|
| `sigma_0` | | | |
| `eta` | | | |
| `Gamma_ph` | | | |
| `R_ph` | | | |
| `sigma_ph` | | | |
| `sigma_15` | | | |

## 7. Dissipation mechanism

```text
internal shocks:
magnetic reconnection:
ambiguous:
```

State the exact magnetization radius and threshold used.

## 8. Alternative low-energy origins

```text
2SBPL:
coupled FS+RS:
broadened photosphere:
spectral evolution:
instrument/systematic:
```

## 9. Model-dependence summary

What changes when the continuum or thermal prescription changes?

## 10. Falsification statement

State the observation that would invalidate the preferred interpretation.
